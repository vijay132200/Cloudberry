You are working on an existing healthcare application. There is a document visibility feature that works correctly in the Patient Portal and Operations Portal, but it is missing from the Doctor Portal. Fix it end to end.

CURRENT WORKING FLOW:
- A patient uploads a document in:
  Patient Portal → Health Records → Documents
- That same document is already visible and downloadable in:
  1. Patient Portal
  2. Operations Portal

WHAT IS BROKEN:
- In the Doctor Portal, when a doctor opens a patient’s profile, the uploaded patient document is not visible.

WHAT I WANT:
A doctor is assigned to multiple patients.
Each patient can upload multiple documents.
Those same patient-uploaded documents must be visible inside the assigned doctor’s portal, for that specific patient only.

REQUIRED DOCTOR PORTAL STRUCTURE:
Doctor Portal
→ Doctor Dashboard
→ Click on any patient
→ Patient profile opens
→ There are multiple tabs inside that profile
→ Add one more tab named “Docs”

Inside this “Docs” tab:
- Show all documents uploaded by that patient from:
  Patient Portal → Health Records → Documents
- Show them in the same structure and design style used in the Patient Portal / Operations Portal
- Each document must be downloadable
- The doctor must only see documents for patients assigned to that doctor

IMPORTANT LOGIC:
- One single uploaded document must be accessible through multiple places:
  1. Patient Portal
  2. Operations Portal
  3. Assigned Doctor’s Portal
- Do not create a separate file copy unless absolutely necessary; prefer linking the same stored document record wherever possible
- Keep the access controlled by doctor-patient assignment
- Do not expose documents of one patient to another patient’s doctor

WHAT TO CHECK AND FIX:
1. Patient upload flow
   - Find exactly where documents are uploaded in the patient portal
   - Trace how the file is stored and how metadata is saved

2. Database / data model
   - Check how documents are stored
   - Check how patient-to-doctor assignment is stored
   - Make sure the doctor portal can query the correct patient documents

3. Backend APIs
   - Check the API used to fetch documents in Patient Portal
   - Check the API used in Operations Portal
   - Build or fix the API used in Doctor Portal so it returns the same patient documents for the assigned doctor

4. Doctor Portal UI
   - Add the new “Docs” tab in the patient profile inside Doctor Portal
   - Fetch and render the patient’s uploaded documents in this tab
   - Make the download action work correctly

5. Permissions / access control
   - Doctor should only see documents of their own assigned patients
   - Another doctor must not see those documents
   - The patient must continue seeing only their own documents
   - Operations Portal must continue working as before

REQUIRED BEHAVIOR FOR MULTIPLE PATIENTS:
- Doctor A may have Patient 1, Patient 2, Patient 3
- When Doctor A opens Patient 1 → Docs tab, only Patient 1’s documents should appear
- When Doctor A opens Patient 2 → Docs tab, only Patient 2’s documents should appear
- Patient 1’s documents must never appear inside Patient 2’s view
- A different doctor assigned to Patient 3 should only see Patient 3’s documents

DO NOT STOP AT THE UI:
Investigate and fix the full chain:
upload → database save → API retrieval → authorization → doctor portal rendering → download

VERIFICATION REQUIRED:
After implementing the fix, verify it properly with real checks.

You must confirm all of the following:

1. Patient Portal verification
- Upload a test document from the patient’s Health Records → Documents section
- Confirm it appears there immediately
- Confirm it remains downloadable

2. Operations Portal verification
- Confirm the same document is still visible in Operations Portal
- Confirm download works there too

3. Doctor Portal verification
- Login as the assigned doctor
- Open the correct patient
- Confirm the new “Docs” tab appears
- Open the Docs tab
- Confirm the patient-uploaded document appears there
- Confirm the document can be downloaded from the doctor portal

4. Multi-patient verification
- Repeat the test for at least 2–3 different patients
- Confirm each doctor sees only their own assigned patients’ documents
- Confirm one patient’s documents do not leak into another patient’s Docs tab
- Confirm an unrelated doctor cannot access those documents

5. Technical verification
- Check the backend response for the Docs tab request
- Confirm the returned document list contains the correct patient’s files
- Confirm the file URL / file ID / download path is valid
- Check network requests and console/server logs if needed
- If tests exist, update or add them
- If tests do not exist, perform manual verification and report exactly what was checked

DELIVERABLES:
- Working code changes
- Root cause of the bug
- Exact files changed
- Explanation of how the document is now shared across Patient Portal, Operations Portal, and Doctor Portal
- Verification proof that the Docs tab works correctly for multiple patients and assigned doctors

Make the Doctor Portal match the same document visibility behavior and structure already working in the Patient Portal and Operations Portal.